import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RegistrovehiculocompletoPage } from './registrovehiculocompleto.page';

describe('RegistrovehiculocompletoPage', () => {
  let component: RegistrovehiculocompletoPage;
  let fixture: ComponentFixture<RegistrovehiculocompletoPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(RegistrovehiculocompletoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
